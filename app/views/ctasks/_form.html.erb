<%= form_with(model: ctask, html: { class: "ctask-form" }) do |form| %>
  <% if ctask.errors.any? %>
    <div class="error-messages">
      <h2><%= pluralize(ctask.errors.count, "error") %> prohibited this task from being saved:</h2>
      <ul>
        <% ctask.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :description %>
    <%= form.text_area :description, class: "form-control", rows: 4 %>
  </div>


  <%= form.submit "Save", id: "third" %>
  
<% end %>
